failure:
	date && date && date && date && date && date && date && date
	date && date && date && date && date && date && date && date
	date && date && date && date && date && date && date && date
	date && date && date && date && date && date && date && date
	date && date && date && date && date && date && date && date
	echo 'crash' && false